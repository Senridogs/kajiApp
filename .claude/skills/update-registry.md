---
description: REGISTRY.mdを再生成して最新状態に更新する
---

# Update Registry

`.claude/REGISTRY.md` を再生成する。

## 手順

1. `node .claude/scripts/update-registry.mjs` を実行
2. 実行結果をユーザーに表示
3. 生成された `.claude/REGISTRY.md` の内容をサマリ表示
