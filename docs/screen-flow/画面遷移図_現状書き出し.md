# 画面遷移図（現状書き出し）

- 作成元: `kajiApp_phase2.pen` の `Flow / 画面遷移図`（`WzwT3`）
- 書き出し日: 2026-02-18
- 目的: 現在の遷移図から「把握できる内容」をテキスト化

## 1. 主要画面ノード

- `SW5Ur` `mainReg`: ユーザー登録
- `p3gup` `mainOnb`: オンボーディング
- `nq9XV` `mainHome`: ホーム
- `BZ20Y` `mainCal`: カレンダー（週）
- `jHAA4` `calMonth`: カレンダー（月表示）
- `yvpsT` `calDrag`: カレンダー（ドラッグ中）
- `CtlPx` `mainLog`: きろく
- `WmsYG` `mainReport`: 月間レポート

## 2. 設定系ノード

- `APk1G` `setSheet`: 設定 BottomSheet（左上ユーザー起点）
- `ryaoL` `setMyReport`: 私のレポート
- `hNFUX` `setPush`: プッシュ通知設定
- `rbLxT` `setFamily`: 家族コード・招待
- `SSIOH` `setManage`: 家事を管理
- `KHhri` `Screen / 家事を管理 / iPhone16`: 家事管理画面（別配置）
- `Zs60u` `setManage`: 画面名は `setManage` だが中身はおやすみモード系シート

## 3. アクション/シート系ノード

- `Bjltk` `aAdd`: 家事追加シート
- `TnsMe` `aCustom`: カスタムアイコンシート
- `7y4lJ` `aEdit`: 家事編集シート
- `U7ZWV` `aHistory`: 履歴シート
- `ffw30` `aBatch`: 家事まとめて追加
- `k28C0` `aToast`: 完了フィードバックトースト
- `8ATS2` `aUndo`: 取り消し確認
- `LL7VP` `detailDateScreen`: 完了日設定
- `WMEdS` `detailDateScreen`: 完了日設定（別配置）
- `6AYiW` `calWeekReschedule`: 週間タスクの日にち変更

## 4. 明示されている遷移（矢印ノードあり）

- `mainOnb` -> `mainReg`（`arrow_main_onb_reg`）
- `mainReg` -> `mainHome`（`arrow_main_reg_home`）
- `mainHome` -> `setSheet`（`arrow_home_to_settings`）
- `mainCal` -> `calMonth`（`arrow_cal_to_month`）
- `calMonth` -> `calDrag`（`arrow_month_to_drag` が複数）
- `setFamily` -> `setManage`（`arrow_family_to_manage`）
- `setManage` -> `aEdit`（`arrow_manage_to_edit` が複数）
- （おやすみモード側） -> `setMyReport`（`arrow_sleep_to_myreport`）
- `calDrag` -> `calWeekReschedule`（`arrow_drag_to_week_reschedule`）

## 5. 補助ラベルとして読める内容

- `label_drag_to_week_reschedule`: 週間タスクの日にち変更
- `section_main`: 主要導線（タブ/初回導線）
- `section_settings`: 設定導線（左上アイコン起点）
- `section_actions`: Home/管理から開くシート・ダイアログ
- `section_detail`: 補助フロー（完了日設定）

## 6. 現状メモ（図面上の注意）

- 同名/重複ノードが一部存在する（例: `setManage`, `detailDateScreen`, `arrow_manage_to_edit`）。
- 一部矢印は回転付きで配置されており、厳密な接続先は近傍ノードからの解釈が必要。
- 本書き出しは「現在の図面に存在するノードと矢印」を優先し、要件書の理想遷移とは分けて記載している。

